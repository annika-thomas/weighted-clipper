[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "weighted-clipper"
version = "0.1.0"
description = "Minimal weighted CLIPPER example"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["weighted_clipper*"]
